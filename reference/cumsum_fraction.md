# Add a fraction of lag to vector

Add a fraction of lag to vector

## Usage

``` r
cumsum_fraction(x, fraction)
```

## Examples

``` r
if (FALSE) { # \dontrun{
cumsum_fraction(rnorm(10), 0.5)
cumsum_fraction(rnorm(1), 0.1)
} # }
```
